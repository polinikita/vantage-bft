apiVersion: 1

datasources:
  - name: Fixed-UID-vantage
    # Keep the provisioned UID aligned with dashboard panel references.
    uid: Fixed-UID-vantage
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    jsonData:
      httpMethod: POST
      manageAlerts: true
      prometheusType: Prometheus
      disableRecordingRules: true
